{
  "name": "queueplay-root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "npm install serve && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "start": "npx serve frontend/dist -s -p $PORT"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
