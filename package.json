{
  "name": "queueplay-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
