{
  "name": "queueplay-root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend install",
    "start": "npm --prefix frontend start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
