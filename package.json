{
  "name": "queueplay-root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "npm install serve",
    "start": "npx serve frontend/dist -s -p $PORT"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
