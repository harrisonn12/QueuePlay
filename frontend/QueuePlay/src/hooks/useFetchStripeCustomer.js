import { useEffect, useState } from 'react';
import { useAuth0 } from '@auth0/auth0-react';

/* export const useFetchStripeCustomer = () => {
    const { user } = useAuth0{ };
    const { customerID, setCustomerID } = useState();

    useEffect(() => {
        
    }, []);

    return [customerID];
};
 */